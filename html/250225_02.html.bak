<!DOCTYPE html>
<html lang="en">
 <head>
  <meta charset="UTF-8">
  <title>Document</title>
  <script>
	function randomNotice(){
		var msg=new Array(5);
		msg[0]='지각을 하지 맙시다.';
		msg[1]='결석을 하지 맙시다.';
		msg[2]='복습을 철저히 합시다.';
		msg[3]='건강관리 유의합시다.';
		msg[4]='친구를 괴롭히지 맙시다.';
		
		var num=Math.floor(Math.random()*5);
		
		// document.write(msg[num]);
		// document.all.noticediv.innerHTML=msg[num];
		var nodiv=document.getElementById('noticediv');
		nodiv.innerHTML=msg[num];
		// 일반적인 태그 객체
		// 1.innerHTML
		// 2.innerText
		
		// window.setTimeout() : 브라우저 객체가 가지고 있는 내장 기능
		// 지정된 시간 후 원하는 함수를 호출하는 기능
		// 형식 : window.setTiomout('함수명',시간); 시간:ms
		
		window.setTimeout('randomNotice',2000);
	}
  
  
  </script>
 </head>
 <body onload="randomNotice();">
	<label>공 지 사 항</label>
	<div id="noticediv" style="border:2px solid darkblue;font-size:20px;width:250px;">

	</div>
 </body>
</html>
